<article id="categories_new">
  <section class="content">
    <section class="interior p4">
			<%= form_for(@category, url: admin_categories_url, method: :post) do |f| %>
				<%= render "form", f: f %>
				<%= f.submit :create %>
			<% end %>
    </section>
  </section>
</article>
